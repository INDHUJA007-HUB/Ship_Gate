# ADR 0003: remediation is PR-only

**Status:** Accepted

First Commit may eventually propose a patch and open a reviewable pull request.
It must not commit directly to a user's branch, auto-merge a pull request, or
deploy infrastructure without an explicit human approval step.

