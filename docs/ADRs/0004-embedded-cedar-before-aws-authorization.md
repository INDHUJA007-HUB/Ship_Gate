# ADR 0004: embedded Cedar locally; AWS authorization later

**Status:** Accepted

Governance policies must work without an AWS account, so local development uses
embedded Cedar. Amazon Verified Permissions is a later deployment integration,
not a Phase 1 runtime dependency.

